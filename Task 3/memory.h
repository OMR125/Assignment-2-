#include <bits./stdc++.h>

#ifndef VOLESIMULATER_MEMORY_H
#define VOLESIMULATER_MEMORY_H
using namespace std;

class memory {
public:
    string cell[256];

    void fetch(string filename);

    void fill();
};

#endif
